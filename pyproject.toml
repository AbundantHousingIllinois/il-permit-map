[project]
name = "il-map-permits"
version = "0.1.0"
description = "Illinois housing permits dashboard for Abundant Housing Illinois"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "pandas>=2.0",
    "shapely>=2.0",
    "playwright>=1.40",
    "pyshp>=3.1.6",
    "topojson>=1.10",
]

[tool.uv]
package = false
